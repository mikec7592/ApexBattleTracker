import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const GamesScreen = () => {
    return <Text style={{ fontSize: 30 }}> GamesScreen</Text>
};

const styles = StyleSheet.create({});

export default GamesScreen;
 